# Replay QA FullStory session capture — build-time, baked into the browser bundle.
# FullStory org ids are public page-source values, so this is safe to commit.
VITE_FULLSTORY_ORG_ID=o-250JSR-na1

# REPLAY_QA_SESSION_TOKEN is intentionally NOT set here. It is server-only (read by
# server.mjs at runtime) and must never be committed. See README "FullStory session capture".
